import React from 'react';

const Input = (CustomLabel) => {
    return (
        <div className="input">
            <input />
            <label>{CustomLabel}</label>
        </div>
    )
}

export default Input;